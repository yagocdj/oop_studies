package questions;

public class Solution {
	
	public Solution() {}
	
	public int removeDuplicates(int[] nums) {
		int i, j, currentDiff;
		i = j = 0;
		
		while (i < nums.length) {
			currentDiff = i - j
			if (currentDiff > 2 && nums[i] == nums[j])
				
		}
	}
}
